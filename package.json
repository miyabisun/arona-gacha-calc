{
  "name": "arona-gacha-calc",
  "version": "1.0.0",
  "private": true,
  "description": "Blue Archive recruitment probability calculator",
  "scripts": {
    "build": "npm run build:faq && npm run build:comparison",
    "build:faq": "node scripts/calculate.js",
    "build:comparison": "node scripts/compare.js",
    "test": "node --test scripts/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
